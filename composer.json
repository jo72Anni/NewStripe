{
    "name": "jo72anni/stripe-cart-checkout",
    "description": "Carrello e checkout con Stripe Payment e database PostgreSQL",
    "type": "project",
    "keywords": ["stripe", "payments", "ecommerce", "postgresql", "checkout"],
    "require": {
        "stripe/stripe-php": "^14.0",
        "ext-curl": "*",
        "ext-json": "*",
        "ext-mbstring": "*",
        "ext-pdo": "*",
        "ext-pdo_pgsql": "*",
        "ext-zip": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.0"
    },
    "license": "MIT",
    "authors": [
        {
            "name": "jo72Anni",
            "email": "tua@email.com",
            "role": "Developer"
        }
    ],
    "autoload": {
        "psr-4": {
            "StripeCart\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "echo '=== 🛒 Stripe Cart Checkout ==='",
            "echo '📦 Stripe PHP SDK: ^14.0'",
            "echo '🗄️  Database: PostgreSQL'",
            "echo '🔧 Verifica estensioni PHP...'",
            "php -r \"\$missing = []; foreach(['pdo', 'pdo_pgsql', 'curl', 'json', 'mbstring', 'zip'] as \$ext) { if (!extension_loaded(\$ext)) { \$missing[] = \$ext; } } if (empty(\$missing)) { echo '✅ Tutte le estensioni sono caricate' . PHP_EOL; } else { echo '❌ Estensioni mancanti: ' . implode(', ', \$missing) . PHP_EOL; }\"",
            "echo '🚀 Sistema pronto per il deploy!'"
        ],
        "post-update-cmd": [
            "echo '📥 Dipendenze aggiornate con successo'"
        ],
        "check-env": [
            "php -r \"\$required = ['STRIPE_SECRET_KEY', 'DB_HOST', 'DB_NAME', 'DB_USER', 'DB_PASSWORD']; \$missing = []; foreach(\$required as \$env) { if (empty(getenv(\$env))) { \$missing[] = \$env; } } if (empty(\$missing)) { echo '✅ Tutte le variabili d\\'ambiente sono configurate' . PHP_EOL; } else { echo '❌ Variabili mancanti: ' . implode(', ', \$missing) . PHP_EOL; }\""
        ],
        "check-system": [
            "@check-env",
            "@check-extensions"
        ]
    },
    "scripts-descriptions": {
        "check-env": "Verifica le variabili d'ambiente necessarie",
        "check-system": "Verifica completo sistema (env + estensioni)",
        "check-extensions": "Verifica estensioni PHP richieste"
    },
    "config": {
        "platform-check": false,
        "sort-packages": true,
        "optimize-autoloader": true,
        "preferred-install": {
            "*": "dist"
        },
        "process-timeout": 0
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
